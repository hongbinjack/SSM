<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mg.mybatis.mapper.UserMapper">

<!--    User getUserByUsername(String name);-->
<select id="getUserByUsername" resultType="User">
    select * from t_user where  username=#{username};
</select>

<!--    User checkLogin(String name,String password);-->
<select id="checkLogin" resultType="User">
    <!--arg0,arg1对应解释，在接口UserMapper中的2 -->
<!--   select * from t_user where username=#{param1} and password=#{hongbin1123}-->
    select * from t_user where username='${param1}' and password='${param2}'
</select>

<!--    User checkLoginByMap(Map<String,Object> map);-->
    <select id="checkLoginByMap" resultType="User">
        select * from t_user where username = #{username} and password = #{password}
    </select>

<!--    void insertUser(User user);-->
    <insert id="insertUser">
        insert into t_user values(null,#{username},#{password},#{age},#{gender},#{email})
    </insert>

<!--    User checkLoginByParam(@Param("username")   String username, @Param("password") String password);-->
<select id="checkLoginByParam" resultType="User">
    select * from t_user where username = #{username} and password = #{password}
</select>

<!--    void updateUser();-->
    <update id="updateUser">
        update t_user set username='Mark',password='MarkOfPassword' where id=10;
    </update>
</mapper>